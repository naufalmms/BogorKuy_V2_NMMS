<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.s.m.marzuk.n.bogorkuy">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo123"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home" />
        <activity android:name=".Menu" />
        <activity android:name=".detailBacaLagi" />
        <activity android:name=".RatethisApp" />
        <activity android:name=".TentangAplikasiIni" />
        <activity android:name=".SejarahBogor"></activity>
    </application>

</manifest>